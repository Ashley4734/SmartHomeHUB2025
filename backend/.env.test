# Test Environment Configuration
# This file is used for running tests

# Server Configuration
NODE_ENV=test
PORT=3001
HOST=localhost

# Database - Use in-memory database for tests
DB_PATH=:memory:

# Security - Use test secrets (not for production!)
JWT_SECRET=test-secret-for-testing-do-not-use-in-production
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=4

# CORS
CORS_ORIGIN=*

# Rate Limiting - More permissive for tests
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10000

# Logging
LOG_LEVEL=error

# Brute Force Protection - Shorter timeouts for tests
MAX_FAILED_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MS=60000
ATTEMPT_WINDOW_MS=60000

# AI Providers - Disabled for tests
OLLAMA_ENABLED=false
OPENAI_ENABLED=false
CLAUDE_ENABLED=false
GEMINI_ENABLED=false

# Protocols - Disabled for tests
ZIGBEE_ENABLED=false
MATTER_ENABLED=false
VOICE_ENABLED=false

# Redis - Disabled for tests
REDIS_ENABLED=false
